<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.OrderMainMapper">
    <resultMap id="BaseResultMap" type="com.shop.entity.OrderMain">
        <id column="id" property="id"/>
        <result column="order_no" property="orderNo"/>
        <result column="user_id" property="userId"/>
        <result column="total_amount" property="totalAmount"/>
        <result column="status" property="status"/>
        <result column="receiver_name" property="receiverName"/>
        <result column="receiver_phone" property="receiverPhone"/>
        <result column="receiver_address" property="receiverAddress"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO order_main (order_no, user_id, total_amount, status, receiver_name, receiver_phone, receiver_address)
        VALUES (#{orderNo}, #{userId}, #{totalAmount}, #{status}, #{receiverName}, #{receiverPhone}, #{receiverAddress})
    </insert>
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM order_main WHERE id = #{id}
    </select>
    <select id="selectByOrderNo" resultMap="BaseResultMap">
        SELECT * FROM order_main WHERE order_no = #{orderNo}
    </select>
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM order_main WHERE user_id = #{userId} ORDER BY id DESC
    </select>
    <update id="updateStatus">
        UPDATE order_main SET status = #{status} WHERE id = #{id}
    </update>
</mapper>
