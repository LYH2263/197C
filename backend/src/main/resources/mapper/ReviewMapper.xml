<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shop.mapper.ReviewMapper">
    <resultMap id="BaseResultMap" type="com.shop.entity.Review">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="product_id" property="productId"/>
        <result column="order_id" property="orderId"/>
        <result column="rating" property="rating"/>
        <result column="content" property="content"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review (user_id, product_id, order_id, rating, content)
        VALUES (#{userId}, #{productId}, #{orderId}, #{rating}, #{content})
    </insert>
    <select id="selectByProductId" resultMap="BaseResultMap">
        SELECT * FROM review WHERE product_id = #{productId} ORDER BY id DESC
    </select>
    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM review WHERE user_id = #{userId} ORDER BY id DESC
    </select>
    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM review ORDER BY id DESC
    </select>
    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM review WHERE id = #{id}
    </select>
    <delete id="deleteById">
        DELETE FROM review WHERE id = #{id}
    </delete>
    <select id="selectByUserAndOrderAndProduct" resultMap="BaseResultMap">
        SELECT * FROM review WHERE user_id = #{userId} AND order_id = #{orderId} AND product_id = #{productId}
    </select>
</mapper>
